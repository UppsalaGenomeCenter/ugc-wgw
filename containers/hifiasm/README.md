hifiasm: haplotype-resolved de novo assembler for PacBio HiFi reads (trio binning with yak
tables, `-1`/`-2`). Upstream https://github.com/chhylp123/hifiasm, MIT licence. Built from
the release tarball with the stock Makefile; runs as the `ugc_wgw_hifiasm_assemble` task of the
`assembly` stage.
